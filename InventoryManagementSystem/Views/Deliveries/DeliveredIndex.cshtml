@using System.Linq
@model List<InventoryManagementSystem.Controllers.DeliveryGroupViewModel>

<div class="container mt-5">

    <!-- Breadcrumb -->
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-light p-2 rounded shadow-sm">
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Orders")">Orders</a></li>
            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Deliveries")">Deliveries</a></li>
            <li class="breadcrumb-item active" aria-current="page">Delivered Orders</li>
        </ol>
    </nav>

    <h2 class="mb-4">Delivered Orders</h2>

    <div class="row g-4">
        @foreach (var group in Model)
        {
            <div class="col-md-4">
                <div class="card shadow-sm border-success h-100">
                    <div class="card-header bg-success text-white fw-bold">
                        Order #@group.OrderId - @group.CustomerName
                    </div>

                    <div class="card-body">
                        <ul class="list-group list-group-flush mb-3">
                            @foreach (var d in group.Products)
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center bg-light text-dark fw-semibold">
                                    <span>@d.OrderDetail?.Product?.Name</span>
                                    <span class="badge bg-success rounded-pill">
                                        @d.OrderDetail?.Quantity × @d.OrderDetail?.UnitPrice
                                    </span>
                                </li>
                            }
                        </ul>

                        <div class="mb-2">
                            <h6 class="fw-bold">Delivered on:</h6>
                            <p class="mb-0">@group.Products.First().DeliveryDate.ToShortDateString()</p>
                        </div>

                        @if (!string.IsNullOrEmpty(group.Products.First().Notes))
                        {
                            <div class="mb-2">
                                <h6 class="fw-bold">Note:</h6>
                                <p class="mb-0">@group.Products.First().Notes</p>
                            </div>
                        }
                    </div>

                    <div class="card-footer text-muted small">
                        Completed Delivery
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .card:hover {
        transform: translateY(-3px);
        transition: all 0.2s ease-in-out;
    }

    .list-group-item span {
        font-weight: 500;
    }

    .badge {
        font-size: 0.9rem;
    }

    .card-header {
        font-size: 1rem;
    }

    .fw-semibold {
        font-weight: 500;
    }
</style>
